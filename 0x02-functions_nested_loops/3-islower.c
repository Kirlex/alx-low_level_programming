#include "main.h"

/**
 * _is lower - checks for lowercase characters
 * Return: 1 if lowercase, 0 if uppercase
 */

int _islower(int c)
{
	if (c >= 'a' && <= 'z')
	{
		return (1);
	}
	else
	{
		retuen (0);
	}
	_putchar('\n');
}
